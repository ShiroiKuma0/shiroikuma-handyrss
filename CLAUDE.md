# shiroikuma-handyrss — project context for Claude Code

A personal rebranded fork of [yanus171/Handy-News-Reader](https://github.com/yanus171/Handy-News-Reader) (itself a Flym fork), built and run by the user on a Huawei Mate XT (Android 12+, microG). Co-installs beside the official app as `shiroikuma.handyrss`. Built on Tuxedo OS with OpenJDK 21.

## REQUIRED READING before any change

Before any change on this repo, read **`.claude/skills/handy-rss-build/SKILL.md`** in full. It contains the project identity, the build-test-confirm-push workflow, the full feature history (commits 1–37), and the current open work. The skill's Workflow + Hard rules sections at the top encode lessons learned (build cache, dirty-index recovery, version-bump discipline, on-device verification) — they exist for specific, documented reasons.

## At-a-glance conventions (full detail in the skill)

- **Branch model:** all changes on the **`custom`** branch. `master` mirrors `upstream/master` (yanus171) fast-forward only and never carries our changes. `origin` is `git@github.com:ShiroiKuma0/shiroikuma-handyrss.git` (SSH push).
- **Versioning:** `versionName = 1.1.6+NNN` (**`N` zero-padded to three digits**), `versionCode = 3410000+N` (plain integer). **Every build bumps N**, no exceptions. The counter lives at `$HOME/.handyrss_build_no` — outside the repo, never tracked. It resets to 0 whenever the adopted upstream base version changes.
- **Upstream tracking:** **always rebase to the latest untagged version** (白い熊's standing rule, 2026-07-25) — if `upstream/master` carries a newer `versionName` than ours, adopt it even though no release tag exists. Never wait for the tag.
- **Reset before applying patches:** `git reset --hard HEAD`, **not** `git checkout -- .`. The latter restores from the index, so a dirty index from an aborted prior push restores an already-patched tree and the next `git apply` fails. `git status --short` should be empty after the reset.
- **Build-test-confirm-push:** changes are tested on the Mate XT before being committed. Once a stage's scope is confirmed, **build automatically** (no per-build "shall I build?" prompt — see the auto-build auto-memory); the build step bumps the counter, builds the APK, copies it to `~/tmp/`, and **auto-delivers via the global `/after-build` skill** (`/adb-check` UNSANDBOXED → `/adb-push` to `/sdcard/tmp/` if a phone is connected, else `/scp` to `skhw:~/tmp/`). Still show the diff before building, and **commit + push only after the user confirms on-device** with "Push" / "good" / "confirmed".

## External state — outside this repo, must be preserved across sessions

- **`$HOME/.handyrss_build_no`** — the build counter. **Currently `8`** (reset to 0 when the base moved `1.1.5` → `1.1.6` on the 2026-08-17 `v1.1.6` rebase; `+008` = the 2026-09-11 restore-merge fix; the previous base's last build was `1.1.5+9`). Never commit.
- **`~/.android-keystores/handyrss-custom.jks`** — signing keystore (alias `handyrss`). Without it, builds cannot be signed.
- **`~/tmp/`** — APK archive directory. Never committed; persists across sessions.
- **`~/git/shiroikuma-handyrss`** — the local working clone (this repo).

## State checkpoint (2026-08-17)

- **Rebased onto the `v1.1.6` release** (2026-08-17) — base moved `1.1.5`/`3400000` → `1.1.6`/`3410000`, counter reset to `0`. Shipped **`1.1.6+003`** (`+001` built before the duplicate-`WAKE_LOCK` dedup, `+002` before the build number was zero-padded). Previous base's last built + pushed version was `1.1.5+9` (`+6` export completeness, `+7` `CANCEL_EXPORT`, `+8` the ghost sweep, `+9` progress `item`/heartbeat/bytes + the export moved into a foreground service).
- **The export artefact is now a ZIP, not an OPML** (commit 40) — `shiroikuma-handyrss_<ts>.zip`. Import still accepts old plain-OPML backups. Since commit 41 it also carries `articles.backup`, `html/`, `images/` and `fonts/` — **nine categories, all defaulting `on`** — and is written as `.part` + rename.
- **The 保存復元 receiver round-trip is now exercised in anger** — 自由作業盤 drives `LIST_CATEGORIES` and `EXPORT_STATE` with live progress (screenshot 2026-07-28: "区分 4/9 — Downloaded images 883/3680"). Still never exercised: the `path` override and an `items` subset.
- **Not yet measured:** whether the ~3680 image files are orphans or belong to live articles. The test is to run **delete old articles** from the entry-list menu (fires `FROM_DELETE_OLD`, so purge + sweep run at once) and compare the image count on the next export. Commit 42 stops the leak going forward but does not clean up retroactively.
- **The `custom` stack (66 commits) sits on the `v1.1.6` tag @ `8ed995eb`** (2026-08-17 release rebase) — for the first time since the master-tip refreshes (`9c1c6dd4` 2026-07-25, `726fefe3` 2026-07-24, `3e885f98` 2026-07-16) the base is a real tag again, because `v1.1.6` *is* the master tip (nothing untagged beyond it). 51 upstream commits, 45 of them Crowdin churn. The functional ones: **`LongOper` extracted out of `FetcherService` into its own class with a wakelock** (`bc374d85` — `mCancelRefresh`/`isCancelRefresh`/`cancelRefresh`/`createCancelPI` moved with it, call sites became `new LongOper(…)`), **full-width images restricted to books** and `GetCSS` turned from static into an instance method (`815da16c`), the **"Screen keep-on duration" setting** + `ScreenOnKeeper` (`7ab229e8`), and an `FB2.extractNotes` fix (`162d6c70`). A repo-wide LF→CRLF conversion (`c3618f47`, 514 files) was **undone by the very next commit**, so it cost the rebase nothing but 374 cosmetic mode changes. Conflicts, all predicted: the `build.gradle` version block (our stamp kept), the end-of-file appends in `values/strings.xml` + `values/arrays.xml` (both sides kept), `WebEntryContent.GetCSS` (upstream's instance signature adopted, our body/heading font logic kept), and the brand purge vs fresh Crowdin translations in 11 `values-*/strings.xml` (purge upheld; in `values-zh` upstream's new `app_name` "Handy News Reader" was rebranded rather than allowed to override the label).
- **Semantic fixup the rebase could not do textually:** our ghost-sweep call site in `FetcherService` had to become `new LongOper(R.string.deleting_ghost_entries, FetcherService::deleteGhost, service)` — git merged the line cleanly but it would not have compiled. Everything else pointing at the moved API (`OPML`, `HTMLParser.kt`, `OneWebPageParser.kt`, `AutoWorker.kt`, `BroadcastActionReciever.kt`, `EntryActivity`) upstream retargeted itself.
- **Recent feature commits (34–43; full detail in the skill's feature-commits list):** the state ZIP now exports articles, downloaded text/images and the font files, and the export is cancellable (`CANCEL_EXPORT`, `.part` + rename); orphaned images/article files are reaped on feed and group deletion, the delete-old job no longer requires the charger; and the export now names the running row (`item`), keeps a heartbeat through the long articles write, reports bytes, and runs in a foreground service with a wakelock instead of inside the receiver. Earlier: uniform grid-card height; mark entries read on leaving the reading view even when starred; the UI-screen kxkb restyle + category Export/Import panel (`1.1.4+46`); the 2026-07-25 rebase + `1.1.5` base adoption; **long-press the top-right ⋮ in the entry list → 白い熊 Handy RSS UI screen** (`EntriesListFragment.SetupMoreOptionsLongPress`, third entry point beside the drawer settings icon and the top-left actionbar icon).
- The `v1.1.6` rebase is **published and confirmed**: `custom` force-pushed, released as `1.1.6+003`, and the `custom-pre-v1.1.6` backup branch deleted (2026-08-17). The pre-rebase tip `0151903f` needs no backup branch — the published **`1.1.5+9` release tag points at exactly that commit**, so the old history stays reachable on origin. Worth remembering for the next rebase: when the last build was released, its tag already *is* the backup.
- **Working agreements** (also in auto-memory): build automatically once a stage's scope is confirmed (no "shall I build?" prompt); strict UI-screen indentation discipline; the Thunderbird OPML format + testing gotchas.
- **Open work:**
  1. **D2f shelved** (2026-05-24): app-wide chrome body text (`textColorPrimary`/`Secondary`), settings row/category backgrounds, control accents. The dialog tail is now DONE (commit 26). Revisit only if living with custom chrome colors makes the mismatch nag.
  2. Open question (deferred with D2f): whether to consolidate all colors under the "白い熊 Handy RSS UI" screen.

## Note on the keystore password

The keystore passphrase (`handyrss123`) is currently embedded in the skill's build commands, which means it's public via this repo. The signing keystore file itself is local-only at `~/.android-keystores/handyrss-custom.jks`, so without the keystore the password is useless — practical risk is low for a personal fork. If hygiene matters, move it into a gitignored `keystore.properties` and reference via `-PKEYSTORE_PASSWORD=$(...)`. Otherwise leave as-is.

## Commit convention — no Claude attribution

Do **not** add any `Co-Authored-By: Claude …` trailer — nor a "🤖 Generated with Claude Code" / Anthropic-attribution line — to commit messages or PR bodies in this repo. 白い熊 does not want Claude attribution in the history; this **overrides** the harness's default to append such a trailer. End commit messages at the last line of the body. (The existing history was scrubbed of these trailers on 2026-06-08; the global rule lives in `~/.claude/CLAUDE.md`.)
